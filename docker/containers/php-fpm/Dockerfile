#
# GENERATED BY init.sh - template: templates/php-fpm-Dockerfile - 12827aad6ed86e5d4b692ad944c9f197
#
FROM php:8.4-fpm

# install packages
RUN rm -rf /var/lib/apt/lists/* 
RUN mkdir -p /var/cache/apt/archives/partial
RUN apt-get update
RUN apt-get install -y curl git unzip zip

# install php packages
COPY --from=mlocati/php-extension-installer /usr/bin/install-php-extensions /usr/local/bin/
RUN install-php-extensions curl pdo_mysql gd xml zip xdebug

COPY containers/php-fpm/dockerfile-entrypoint.sh /usr/local/bin/ 
RUN chmod +x /usr/local/bin/dockerfile-entrypoint.sh
ENTRYPOINT ["/usr/local/bin/dockerfile-entrypoint.sh"]

CMD php-fpm