#
# GENERATED BY init.sh - template: ./templates/Dockerfile - b7f8a5efe7db3a1d1e4260169031426f
#
FROM php:8.1-apache

# braucht der Crawler nicht:
# RUN a2enmod rewrite

RUN apt-get update && apt-get install -y git unzip zip

# WORKDIR /var/www/ahcrawler/public_html
WORKDIR /var/www/ahcrawler


COPY --from=mlocati/php-extension-installer /usr/bin/install-php-extensions /usr/local/bin/
# RUN install-php-extensions gd pdo_mysql bcmath zip intl opcache
RUN install-php-extensions curl pdo_mysql mbstring xml zip xdebug


# create shared group id (local machine and inside container)
# RUN addgroup --gid {{WEBGID}} sharedgroup
# RUN usermod -a -G sharedgroup {{WEBUSER}}
# USER {{WEBUSER}}